{
  "name": "express-intro",
  "version": "1.0.0",
  "description": "## NPM To initialize with npm, the command is 'npm init'     - makes a package.json     - to avoid questions the command is 'npm init --yes'",
  "main": "index.js",
  "scripts": {
    "start": "node server/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
